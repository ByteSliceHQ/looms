{
  "name": "@looms/actor",
  "version": "0.0.1",
  "private": true,
  "description": "Actor cell contract and per-run single-writer host for Looms",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/ByteSliceHQ/looms.git",
    "directory": "packages/actor"
  },
  "files": [
    "dist",
    "src"
  ],
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "bun x tsc -p tsconfig.build.json",
    "check-types": "bun x tsc -p tsconfig.build.json --noEmit",
    "test": "bun test src"
  },
  "dependencies": {
    "@looms/core": "workspace:*",
    "@looms/runtime": "workspace:*",
    "effect": "4.0.0-rc.112"
  },
  "devDependencies": {
    "@looms/workflow": "workspace:*",
    "@types/bun": "^1.3.9",
    "typescript": "7.0.2"
  }
}
