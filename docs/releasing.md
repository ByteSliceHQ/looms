# Releasing

The canonical maintainer workflow is in [`RELEASE.md`](../RELEASE.md).

`@swirls/looms` is the only public package. Changesets maintains its version PR, and the protected
release workflow verifies and publishes the assembled package with npm provenance.
