---
'@swirls/looms': minor
---

Add `@swirls/looms/jev` with `defineJev` (`defineKind('jev', …)`) and `evaluate()` so typed Jev questions can branch a durable workflow the same way `gate()` parks for approval.
